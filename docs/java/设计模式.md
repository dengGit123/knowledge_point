# 设计模式

> 参考资源：
> - [菜鸟教程 - 设计模式](https://www.runoob.com/design-pattern/design-pattern-tutorial.html)
> - [Refactoring.Guru - 设计模式](https://refactoring.guru/zh/design-patterns)
> - [Oracle Java Tutorials](https://docs.oracle.com/javase/tutorial/)

---

## 一、概述

### 1. 什么是设计模式

**设计模式（Design Pattern）** 是前人（软件工程前辈）在长期的软件开发实践中，总结出来的、**可复用的**、针对特定问题的**面向对象设计经验**。

更具体地说：设计模式是一套被反复使用、多数人知晓的、经过分类的、代码设计经验的总结。它描述了在特定场景下，类与对象之间如何组织、如何交互，才能让系统更灵活、可维护、可扩展。

1994 年，四位作者（Erich Gamma、Richard Helm、Ralph Johnson、John Vlissides，合称 **GoF / 四人帮**）出版了《设计模式：可复用面向对象软件的基础》一书，归纳出了经典的 **23 种设计模式**，从此成为面向对象设计的「圣经」。

### 2. 通俗类比

可以把设计模式理解成**建筑工程的图纸模板**：

| 类比对象 | 设计模式中的对应 |
| --- | --- |
| 建筑图纸模板（如「三室一厅户型图」） | 设计模式（如「单例模式」「工厂模式」） |
| 遇到「需要建一栋只有一户的楼」 | 遇到「全局只需要一个实例」的问题 |
| 直接套用成熟的「独栋别墅图纸」 | 直接套用「单例模式」 |
| 照图纸施工，细节可微调 | 按模式骨架编码，具体实现可变化 |

> 💡 **提示：** 设计模式**不是发明出来的算法**，而是**总结出来的经验**。它的价值在于提供了一套「共同词汇」——你说「这里用个工厂模式」，团队成员就立刻明白你的设计意图，无需长篇大论。

### 3. 为什么要学习设计模式

| 收益 | 说明 |
| --- | --- |
| 提高代码可复用性 | 模式鼓励面向接口编程，减少重复 |
| 提高代码可扩展性 | 遵循开闭原则，新增功能不改老代码 |
| 提高代码可读性 | 模式是通用词汇，团队沟通成本降低 |
| 应对变化 | 把「变化的部分」和「稳定的部分」分离 |
| 面试加分项 | 几乎所有 Java 后端面试都会涉及设计模式 |

> ⚠️ **注意：** 设计模式不是银弹。**滥用模式 = 过度设计**，反而让系统更复杂。学习模式是为了「遇到合适的问题时知道有这个工具」，而不是「拿着锤子看什么都像钉子」。

---

## 二、设计模式的六大原则

设计模式背后的灵魂是**面向对象设计原则**。理解了原则，模式就是原则的具体落地。下面用一张总览表先感受六大原则：

| 原则 | 英文缩写 | 一句话核心 | 通俗理解 |
| --- | --- | --- | --- |
| 单一职责原则 | SRP | 一个类只做一个事 | 一个人只干一份工作 |
| 开闭原则 | OCP | 对扩展开放，对修改关闭 | 加新功能不改老代码 |
| 里氏替换原则 | LSP | 子类必须能替换父类 | 儿子能干的活，老爹交给他没问题 |
| 依赖倒置原则 | DIP | 依赖抽象，不依赖具体 | 找对象看人品（抽象），不看具体长相 |
| 接口隔离原则 | ISP | 接口要小而专 | 接口不要塞一堆用不到的方法 |
| 迪米特法则 | LoD | 最少知识，只和直接朋友说话 | 别和陌生人说话 |

下面逐一展开。

### 1. 单一职责原则（Single Responsibility Principle, SRP）

**定义：** 一个类应该只有一个引起它变化的原因，即一个类只负责一项职责。

```java
// ❌ 一个类承担多个职责：既管用户数据，又管用户数据库存储
class UserManager {
    void register(User u) { /* 注册逻辑 */ }
    void login(User u) { /* 登录逻辑 */ }
    void saveToDb(User u) { /* 数据库存取 */ }
    void sendEmail(User u) { /* 发邮件 */ }
}

// ✅ 拆分职责，各司其职
class UserService {           // 业务逻辑
    void register(User u) {}
    void login(User u) {}
}
class UserRepository {        // 数据访问
    void save(User u) {}
}
class EmailService {          // 邮件发送
    void send(User u) {}
}
```

### 2. 开闭原则（Open-Closed Principle, OCP）

**定义：** 软件实体（类、模块、函数等）应该**对扩展开放，对修改关闭**。即：增加新功能时，尽量新增代码，而不是修改老代码。

```java
// ❌ 每新增一种形状都要改 GraphicEditor，违背开闭原则
class GraphicEditor {
    void drawShape(Shape s) {
        if (s.type == 1) drawCircle();
        else if (s.type == 2) drawRectangle();
        // 新增三角形又得改这里……
    }
}

// ✅ 通过抽象 + 多态扩展，新增形状不改老代码
interface Shape { void draw(); }            // 抽象稳定
class Circle implements Shape { public void draw() { /* ... */ } }
class Rectangle implements Shape { public void draw() { /* ... */ } }
// 新增三角形：只新增一个类即可
class Triangle implements Shape { public void draw() { /* ... */ } }
```

### 3. 里氏替换原则（Liskov Substitution Principle, LSP）

**定义：** 所有引用基类（父类）的地方必须能透明地使用其子类的对象。通俗讲：**子类对象替换父类对象后，程序行为不能出错**。

经典反例：正方形不应继承长方形（重写 setter 后破坏了长方形「长宽可独立变化」的契约）。

```java
class Rectangle { protected int w, h; /* getter/setter */ }
// ❌ Square 继承 Rectangle 后，改一边会同时改两边，破坏了父类的行为契约
class Square extends Rectangle {
    @Override public void setW(int w) { this.w = w; this.h = w; }
}
```

### 4. 依赖倒置原则（Dependency Inversion Principle, DIP）

**定义：**
- 高层模块不应该依赖低层模块，二者都应该依赖其抽象。
- 抽象不应该依赖细节，细节应该依赖抽象。

通俗讲：**面向接口编程**，而不是面向实现编程。

```java
// ❌ 高层直接依赖低层具体类，强耦合
class UserService {
    private MySQLUserDao dao = new MySQLUserDao();  // 写死
}

// ✅ 高层与低层都依赖抽象（接口），通过注入解耦
interface UserDao { void save(User u); }
class MySQLUserDao implements UserDao { public void save(User u) {} }
class UserService {
    private final UserDao dao;                     // 依赖抽象
    public UserService(UserDao dao) { this.dao = dao; }  // 构造注入
}
```

### 5. 接口隔离原则（Interface Segregation Principle, ISP）

**定义：** 客户端不应该被迫依赖它不使用的方法。即：**接口要小而专**，不要设计臃肿的接口。

```java
// ❌ 接口太胖，不需要的方法也被迫实现
interface Worker {
    void work();
    void eat();
    void sleep();
}
class Robot implements Worker {
    public void work() {}
    public void eat() { throw new UnsupportedOperationException(); } // 机器人不吃饭
    public void sleep() { throw new UnsupportedOperationException(); }
}

// ✅ 拆成多个细粒度接口
interface Workable { void work(); }
interface Eatable { void eat(); }
class Robot implements Workable { public void work() {} }
class Human implements Workable, Eatable { /* ... */ }
```

### 6. 迪米特法则（Law of Demeter, LoD / 最少知识原则）

**定义：** 一个对象应该对其他对象保持最少的了解。只与「直接朋友」（成员变量、方法参数、方法返回值）通信，不要和「陌生人」说话。

```java
// ❌ 链式深入调用，耦合了太多内部结构
a.getB().getC().doSomething();

// ✅ 让直接朋友（a）提供需要的方法
a.doSomethingViaC();
```

### 7. 合成复用原则（CARP，补充）

**定义：** 尽量使用**对象组合/聚合**，而不是**继承**来达到复用目的。继承是强耦合（「is-a」），组合是松耦合（「has-a」）。

> 💡 **提示：** 六大原则可以浓缩成一句话——**「找出变化，隔离变化，面向接口编程」**。所有的设计模式，本质上都是在用不同的方式落地这句话。

---

## 三、三大分类与 23 种一览表

GoF 把 23 种模式按**目的**分成三大类：

| 分类 | 关注点 | 数量 | 通俗理解 |
| --- | --- | --- | --- |
| **创建型** | 「如何创建对象」 | 5 种 | 把对象的创建过程封装起来 |
| **结构型** | 「如何组合类与对象」 | 7 种 | 像搭积木一样组装结构 |
| **行为型** | 「对象之间如何协作」 | 11 种 | 定义对象间的职责分配与通信 |

### 23 种设计模式一览表

| # | 模式 | 分类 | 一句话用途 | 重要程度 |
| --- | --- | --- | --- | --- |
| 1 | 单例模式（Singleton） | 创建型 | 全局只有一个实例 | ⭐⭐⭐⭐⭐ |
| 2 | 工厂方法（Factory Method） | 创建型 | 子类决定创建哪种对象 | ⭐⭐⭐⭐⭐ |
| 3 | 抽象工厂（Abstract Factory） | 创建型 | 创建一族相关对象 | ⭐⭐⭐⭐ |
| 4 | 建造者模式（Builder） | 创建型 | 分步构建复杂对象 | ⭐⭐⭐⭐⭐ |
| 5 | 原型模式（Prototype） | 创建型 | 通过克隆已有对象创建新对象 | ⭐⭐⭐ |
| 6 | 适配器模式（Adapter） | 结构型 | 让不兼容的接口协同工作 | ⭐⭐⭐⭐⭐ |
| 7 | 装饰器模式（Decorator） | 结构型 | 动态地给对象增加新功能 | ⭐⭐⭐⭐⭐ |
| 8 | 代理模式（Proxy） | 结构型 | 用代理对象控制对原对象的访问 | ⭐⭐⭐⭐⭐ |
| 9 | 外观模式（Facade） | 结构型 | 为复杂子系统提供统一入口 | ⭐⭐⭐⭐ |
| 10 | 享元模式（Flyweight） | 结构型 | 共享细粒度对象以节省内存 | ⭐⭐ |
| 11 | 组合模式（Composite） | 结构型 | 树形结构统一处理个体与整体 | ⭐⭐⭐ |
| 12 | 桥接模式（Bridge） | 结构型 | 分离抽象与实现，独立变化 | ⭐⭐⭐ |
| 13 | 策略模式（Strategy） | 行为型 | 封装可互换的算法族 | ⭐⭐⭐⭐⭐ |
| 14 | 模板方法（Template Method） | 行为型 | 定义骨架，子类填充细节 | ⭐⭐⭐⭐⭐ |
| 15 | 观察者模式（Observer） | 行为型 | 一对多的发布-订阅 | ⭐⭐⭐⭐⭐ |
| 16 | 责任链模式（Chain of Responsibility） | 行为型 | 请求沿链传递直到处理 | ⭐⭐⭐⭐⭐ |
| 17 | 命令模式（Command） | 行为型 | 把请求封装成对象 | ⭐⭐⭐ |
| 18 | 迭代器模式（Iterator） | 行为型 | 顺序访问集合元素 | ⭐⭐⭐⭐ |
| 19 | 状态模式（State） | 行为型 | 状态变化时改变行为 | ⭐⭐⭐⭐ |
| 20 | 中介者模式（Mediator） | 行为型 | 用中介者解耦多对象交互 | ⭐⭐⭐ |
| 21 | 备忘录模式（Memento） | 行为型 | 保存对象状态以支持回滚 | ⭐⭐ |
| 22 | 访问者模式（Visitor） | 行为型 | 在不修改类的前提下增加操作 | ⭐⭐ |
| 23 | 解释器模式（Interpreter） | 行为型 | 解释一种语言的语法 | ⭐ |

> 💡 **提示：** 面试和实际工作中，**加粗的高频模式（约 12 种）**需要重点掌握，其余了解思想即可。本文档重点讲解：**单例、工厂方法、抽象工厂、建造者、适配器、装饰器、代理、外观、策略、模板方法、观察者、责任链**。

---

## 四、创建型模式详解

创建型模式的核心思想是：**把对象的创建和使用解耦**，让系统不依赖对象的具体创建方式。

### 1. 单例模式（Singleton）⭐⭐⭐⭐⭐

**是什么：** 确保一个类在整个程序生命周期中**只有一个实例**，并提供一个全局访问点。

**解决什么问题：** 全局共享的资源（如配置、连接池、日志器、Spring 容器中的单例 Bean），如果到处 `new` 会浪费内存、状态不一致。

**类图简图：**

```
┌───────────────────────┐
│      Singleton        │
├───────────────────────┤
│ - instance: Singleton │  ← 类持有唯一实例（static）
├───────────────────────┤
│ - Singleton()         │  ← 私有构造，禁止外部 new
│ + getInstance()       │  ← 全局访问点（static）
│ + doSomething()       │
└───────────────────────┘
```

#### (1) 饿汉式（线程安全，立即加载）

实例在类加载时就创建。利用类加载机制天然保证线程安全。

```java
public class Singleton1 {
    // 类加载时即创建，JVM 保证线程安全
    private static final Singleton1 INSTANCE = new Singleton1();
    private Singleton1() {}              // 私有构造
    public static Singleton1 getInstance() { return INSTANCE; }
}
```
- 优点：实现简单、线程安全。
- 缺点：类加载即初始化，若未使用会浪费内存；不支持延迟加载。

#### (2) 懒汉式（懒加载，需处理线程安全）

```java
// ❌ 简单懒汉式：多线程下不安全，可能创建多个实例
public class Singleton2 {
    private static Singleton2 instance;
    private Singleton2() {}
    public static Singleton2 getInstance() {
        if (instance == null) {
            instance = new Singleton2();   // 竞态条件！
        }
        return instance;
    }
}

// ✅ 加 synchronized：线程安全，但性能差（每次调用都加锁）
public static synchronized Singleton2 getInstance() {
    if (instance == null) instance = new Singleton2();
    return instance;
}
```

#### (3) 双重检查锁（DCL，Double-Checked Locking）⭐ 推荐

```java
public class Singleton3 {
    // volatile 防止「指令重排序」导致的半初始化对象问题
    private static volatile Singleton3 instance;
    private Singleton3() {}

    public static Singleton3 getInstance() {
        if (instance == null) {                 // 第一次检查：避免不必要的加锁
            synchronized (Singleton3.class) {
                if (instance == null) {         // 第二次检查：确保只创建一次
                    instance = new Singleton3();
                }
            }
        }
        return instance;
    }
}
```

> ⚠️ **注意：** `volatile` 在 DCL 中**必不可少**。`new` 操作分三步：分配内存 → 初始化 → 赋值引用，若被重排成「分配 → 赋值 → 初始化」，其他线程可能拿到未初始化完成的对象。`volatile` 通过内存屏障禁止这种重排。

#### (4) 静态内部类（推荐，延迟加载 + 线程安全）

利用**类加载机制**：外部类加载时不会立即加载静态内部类，只有调用 `getInstance()` 时才加载，天然实现懒加载和线程安全。

```java
public class Singleton4 {
    private Singleton4() {}
    // 静态内部类，持有实例
    private static class Holder {
        private static final Singleton4 INSTANCE = new Singleton4();
    }
    public static Singleton4 getInstance() {
        return Holder.INSTANCE;     // 触发 Holder 类加载，JVM 保证线程安全
    }
}
```

#### (5) 枚举实现（最优，防反射/反序列化攻击）

Effective Java 作者 Josh Bloch 推荐。枚举天然是单例，且天然防御反射攻击和反序列化漏洞。

```java
public enum Singleton5 {
    INSTANCE;                       // 唯一实例
    public void doSomething() {}
}
// 使用：Singleton5.INSTANCE.doSomething();
```

#### 五种实现对比

| 实现方式 | 线程安全 | 懒加载 | 性能 | 防反射/反序列化 | 推荐度 |
| --- | --- | --- | --- | --- | --- |
| 饿汉式 | ✅ | ❌ | 高 | ❌ | ⭐⭐⭐⭐ |
| 懒汉式 synchronized | ✅ | ✅ | 低 | ❌ | ⭐⭐ |
| 双重检查锁 DCL | ✅ | ✅ | 高 | ❌ | ⭐⭐⭐⭐⭐ |
| 静态内部类 | ✅ | ✅ | 高 | ❌ | ⭐⭐⭐⭐⭐ |
| 枚举 | ✅ | ❌ | 高 | ✅ | ⭐⭐⭐⭐⭐ |

---

### 2. 工厂模式（Factory）⭐⭐⭐⭐⭐

**是什么：** 定义一个用于创建对象的接口，让子类决定实例化哪个类。工厂模式把对象的创建逻辑封装起来，调用方无需关心对象怎么 `new`。

工厂模式有三种演进形态：**简单工厂 → 工厂方法 → 抽象工厂**。

#### (1) 简单工厂（Simple Factory / 静态工厂）

**场景类比：** 一家「奶茶店」，你只要说「要珍珠奶茶」，店员（工厂）帮你制作，你不用关心制作过程。

```java
// 抽象产品
interface MilkTea { void drink(); }
// 具体产品
class PearlMilkTea implements MilkTea { public void drink() { System.out.println("珍珠奶茶"); } }
class CoconutMilkTea implements MilkTea { public void drink() { System.out.println("椰果奶茶"); } }

// 简单工厂
class MilkTeaFactory {
    public static MilkTea create(String type) {
        switch (type) {
            case "pearl":   return new PearlMilkTea();
            case "coconut": return new CoconutMilkTea();
            default: throw new IllegalArgumentException("未知类型");
        }
    }
}
// 使用：MilkTea t = MilkTeaFactory.create("pearl");
```
- 缺点：每新增一种奶茶都要修改工厂的 `switch`，违背**开闭原则**。

#### (2) 工厂方法（Factory Method）

**核心改进：** 把工厂也抽象化，每种产品对应一个工厂类。新增产品时只需新增产品类 + 工厂类，**不改老代码**。

```
┌──────────────┐        ┌──────────────────┐
│  <<产品>>    │        │  <<工厂>>        │
│   Product    │◀───创建│   Factory        │
└──────┬───────┘        └────────┬─────────┘
       △                         △
       │ implements              │ implements
┌──────┴───────┐        ┌────────┴─────────┐
│ ConcreteProA │        │ ConcreteFactoryA │──▶ new ConcreteProA()
└──────────────┘        └──────────────────┘
```

```java
// 产品接口 + 工厂接口
interface MilkTea { void drink(); }
interface MilkTeaFactory { MilkTea create(); }

// 珍珠奶茶 + 其工厂
class PearlMilkTea implements MilkTea { public void drink() { System.out.println("珍珠奶茶"); } }
class PearlMilkTeaFactory implements MilkTeaFactory {
    public MilkTea create() { return new PearlMilkTea(); }
}
// 椰果奶茶 + 其工厂
class CoconutMilkTea implements MilkTea { public void drink() { System.out.println("椰果奶茶"); } }
class CoconutMilkTeaFactory implements MilkTeaFactory {
    public MilkTea create() { return new CoconutMilkTea(); }
}

// 使用
MilkTeaFactory factory = new PearlMilkTeaFactory();
factory.create().drink();   // 珍珠奶茶
```
- 优点：符合开闭原则，扩展容易。
- 缺点：类数量翻倍（每加一种产品加两个类）。

#### (3) 抽象工厂（Abstract Factory）

**场景：** 需要创建**一族相关**的产品（如「现代风家具」：现代沙发+现代椅子+现代桌子；「古典风家具」：古典沙发+...）。抽象工厂提供一个接口，创建一系列相关或相互依赖的对象。

```java
// 一族产品接口
interface Chair { void sit(); }
interface Sofa { void lie(); }

// 抽象工厂：创建一整族家具
interface FurnitureFactory {
    Chair createChair();
    Sofa createSofa();
}
// 现代风格工厂
class ModernFurnitureFactory implements FurnitureFactory {
    public Chair createChair() { return new ModernChair(); }
    public Sofa createSofa() { return new ModernSofa(); }
}
// 古典风格工厂
class ClassicalFurnitureFactory implements FurnitureFactory {
    public Chair createChair() { return new ClassicalChair(); }
    public Sofa createSofa() { return new ClassicalSofa(); }
}
```

#### 三种工厂对比

| 维度 | 简单工厂 | 工厂方法 | 抽象工厂 |
| --- | --- | --- | --- |
| 结构复杂度 | 低 | 中 | 高 |
| 扩展新产品 | 需改工厂（违背 OCP） | 新增类（符合 OCP） | 新增风格易，新增产品族难 |
| 创建产品数 | 单一 | 单一 | 一族多个 |
| 适用场景 | 产品少且稳定 | 产品种类多 | 需创建产品族 |

---

### 3. 建造者模式（Builder）⭐⭐⭐⭐⭐

**是什么：** 将一个**复杂对象的构造**与它的**表示**分离，使得同样的构造过程可以创建不同的表示。常用于「分步构建、参数很多」的对象。

**场景类比：** 点一份「套餐」——你不必一次性报出 10 个参数，而是按步骤选择主食、饮料、甜点。

#### 类图简图

```
┌───────────────┐    产品      ┌─────────────┐
│   Director    │─────────────▶│   Product   │
│ (指挥者:按顺序)│              └─────────────┘
└───────┬───────┘                     ▲
        │ 构建                        │
┌───────▼───────┐   逐步设置   ┌──────┴──────┐
│  Builder(抽象)│─────────────▶│ ConcreteBdr │
└───────────────┘              └─────────────┘
```

#### 链式调用（最常见的实战写法）

```java
// 复杂产品：一个 User 对象有很多可选字段
public class User {
    private final String name;       // 必填
    private final int age;           // 可选
    private final String email;      // 可选
    private final String phone;      // 可选

    private User(Builder b) {
        this.name = b.name;
        this.age = b.age;
        this.email = b.email;
        this.phone = b.phone;
    }

    // 静态内部 Builder，返回 this 实现链式调用
    public static class Builder {
        private final String name;
        private int age;
        private String email;
        private String phone;

        public Builder(String name) { this.name = name; }   // 必填项放构造参数
        public Builder age(int age) { this.age = age; return this; }
        public Builder email(String email) { this.email = email; return this; }
        public Builder phone(String phone) { this.phone = phone; return this; }
        public User build() { return new User(this); }
    }
}

// 使用：链式调用，可读性极佳
User user = new User.Builder("张三")
        .age(28)
        .email("zs@example.com")
        .phone("13800000000")
        .build();
```

> 💡 **提示：** Lombok 的 `@Builder` 注解可以自动生成上面的 Builder 代码，实际开发中非常常用。

#### 适用场景

| 场景 | 例子 |
| --- | --- |
| 参数多（>4 个）且多数可选 | 配置对象、HTTP 请求对象 |
| 需要分步构建 | SQL 拼装、HTML 文档构建 |
| 不可变对象 | `String`、`StringBuilder`、`StringBuilder` 本身就是 Builder |

---

### 4. 原型模式（Prototype）⭐⭐⭐（简略）

**是什么：** 通过**克隆（拷贝）**一个已有对象来创建新对象，而不是 `new`。

**场景：** 创建对象成本大（如需要查数据库、网络请求），而已有对象足够相似时。

```java
// 实现 Cloneable，重写 clone()
class User implements Cloneable {
    String name;
    public User(String name) { this.name = name; }
    @Override
    protected User clone() throws CloneNotSupportedException {
        return (User) super.clone();   // 浅拷贝
    }
}
User prototype = new User("张三");
User copy = prototype.clone();         // 克隆出新对象
```

> ⚠️ **注意：** `clone()` 默认是**浅拷贝**（基本类型复制值，引用类型复制引用）。若需深拷贝，要手动递归克隆或用序列化方式。

---

## 五、结构型模式详解

结构型模式关注的是**如何把类或对象组合成更大的结构**，就像搭积木。

### 1. 适配器模式（Adapter）⭐⭐⭐⭐⭐

**是什么：** 将一个类的接口转换成客户希望的另一个接口，使原本**接口不兼容**的类可以一起工作。

**场景类比：** 出国旅游，插座形状不对，需要带一个**转换插头（适配器）**。插头本身不变，适配器负责「翻译」。

#### 类图简图（对象适配器）

```
┌──────────────┐   target    ┌──────────────┐  delegate   ┌──────────────┐
│  Target(目标) │◀───────────│  Adapter     │────────────▶│ Adaptee(被适配)│
│ +request()   │             │ +request()   │             │ +specificReq()│
└──────────────┘             └──────────────┘             └──────────────┘
```

#### 两种实现方式

```java
// 目标接口（客户端期望的接口）
interface Target { void request(); }
// 被适配者（接口不兼容，比如第三方/老系统）
class Adaptee { public void specificRequest() { System.out.println("老接口方法"); } }

// ① 类适配器：通过继承 Adaptee（Java 单继承，受限）
class ClassAdapter extends Adaptee implements Target {
    public void request() { super.specificRequest(); }
}

// ② 对象适配器：通过组合持有 Adaptee（更灵活，推荐）
class ObjectAdapter implements Target {
    private final Adaptee adaptee;                  // 组合
    public ObjectAdapter(Adaptee adaptee) { this.adaptee = adaptee; }
    public void request() { adaptee.specificRequest(); }  // 转发调用
}
```

**典型应用：** `InputStreamReader` 就是把字节流 `InputStream` 适配成字符流 `Reader` 的适配器。

---

### 2. 装饰器模式（Decorator）⭐⭐⭐⭐⭐

**是什么：** 动态地给一个对象**增加额外的职责**，比生成子类（继承）更灵活。

**场景类比：** 买奶茶——基础是「原味」，可以一层层加料（珍珠、椰果、奶盖），每加一层就是一个装饰器，最终组合出你要的口味。

**与继承的区别：** 继承是静态的（编译时确定），装饰器是动态的（运行时组合），且支持多层叠加。

#### 类图简图

```
┌────────────────┐  ◇(聚合自身)  ┌─────────────────┐
│   Component    │◀─────────────│   Decorator     │
│ +operation()   │              │ -component      │
└───────┬────────┘              │ +operation()    │
        △                       └────────┬────────┘
        │ implements                     │ extends
┌───────┴────────┐              ┌────────┴────────┐
│ ConcreteComp   │              │ ConcreteDecoA/B │
│ (被装饰对象)    │              │ (具体装饰器)     │
└────────────────┘              └─────────────────┘
```

#### 代码示例：咖啡加料

```java
// 抽象组件
interface Coffee { double cost(); String desc(); }
// 具体组件：基础咖啡
class SimpleCoffee implements Coffee {
    public double cost() { return 10; }
    public String desc() { return "咖啡"; }
}
// 抽象装饰器：实现了 Coffee 接口，并持有一个 Coffee
abstract class CoffeeDecorator implements Coffee {
    protected final Coffee coffee;
    public CoffeeDecorator(Coffee coffee) { this.coffee = coffee; }
}
// 具体装饰器：加奶
class MilkDecorator extends CoffeeDecorator {
    public MilkDecorator(Coffee c) { super(c); }
    public double cost() { return coffee.cost() + 3; }       // 在原价上加 3
    public String desc() { return coffee.desc() + " +奶"; }
}
// 具体装饰器：加糖
class SugarDecorator extends CoffeeDecorator {
    public SugarDecorator(Coffee c) { super(c); }
    public double cost() { return coffee.cost() + 1; }
    public String desc() { return coffee.desc() + " +糖"; }
}

// 使用：层层装饰
Coffee c = new SugarDecorator(new MilkDecorator(new SimpleCoffee()));
System.out.println(c.desc() + " 共 " + c.cost() + " 元");
// 输出：咖啡 +奶 +糖 共 14 元
```

#### 在 Java IO 中的应用

Java IO 流是装饰器模式的经典应用：

```java
// 一层层装饰：FileInputStream（读字节） → BufferedInputStream（加缓冲） → DataInputStream（读基本类型）
InputStream in = new DataInputStream(
                     new BufferedInputStream(
                         new FileInputStream("a.txt")));
```

| IO 类 | 角色 |
| --- | --- |
| `InputStream` | 抽象组件 Component |
| `FileInputStream` | 具体组件 ConcreteComponent |
| `FilterInputStream` | 抽象装饰器 Decorator |
| `BufferedInputStream`、`DataInputStream` | 具体装饰器 ConcreteDecorator |

---

### 3. 代理模式（Proxy）⭐⭐⭐⭐⭐

**是什么：** 为其他对象提供一个**代理**以控制对这个对象的访问。代理对象在客户端和目标对象之间起到中介作用，可以在不修改目标对象的前提下增加额外功能（如权限校验、日志、延迟加载、缓存）。

**场景类比：** 房东（目标对象）不直接面对租客，而是通过**中介（代理）**出租房子。中介可以在租房前后加上「带看房、签合同、收押金」等附加动作。

#### 类图简图

```
┌────────────────┐  uses   ┌────────────────┐  delegates   ┌────────────────┐
│    Client      │────────▶│   Proxy        │─────────────▶│   RealSubject  │
│                │         │ -realSubject   │              │ (目标对象)      │
└────────────────┘         │ +request()     │              │ +request()     │
                           └────────────────┘              └────────────────┘
                                  △
                                  │ 实现同一接口
                           ┌────────────────┐
                           │   Subject      │
                           │ +request()     │
                           └────────────────┘
```

#### (1) 静态代理

```java
interface UserService { void save(); }
// 目标对象
class UserServiceImpl implements UserService {
    public void save() { System.out.println("保存用户"); }
}
// 代理对象：与目标实现同一接口，在前后增加日志
class UserServiceProxy implements UserService {
    private final UserService target;
    public UserServiceProxy(UserService target) { this.target = target; }
    public void save() {
        System.out.println("[日志] 方法开始");      // 前置增强
        target.save();                              // 调用目标
        System.out.println("[日志] 方法结束");      // 后置增强
    }
}
// 使用
new UserServiceProxy(new UserServiceImpl()).save();
```
- 缺点：每个目标类都要手写一个代理类，类爆炸。

#### (2) JDK 动态代理（基于接口）

利用反射在**运行时**动态生成代理类，无需手写。要求目标对象**必须实现接口**。

```java
import java.lang.reflect.*;

// 通用调用处理器：可代理任意接口
class LogInvocationHandler implements InvocationHandler {
    private final Object target;
    public LogInvocationHandler(Object target) { this.target = target; }

    @Override
    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
        System.out.println("[日志] " + method.getName() + " 开始");
        Object result = method.invoke(target, args);   // 反射调用目标方法
        System.out.println("[日志] " + method.getName() + " 结束");
        return result;
    }
}

// 使用：Proxy.newProxyInstance 生成代理对象
UserService target = new UserServiceImpl();
UserService proxy = (UserService) Proxy.newProxyInstance(
        target.getClass().getClassLoader(),
        target.getClass().getInterfaces(),
        new LogInvocationHandler(target));
proxy.save();
```

#### (3) CGLIB 动态代理（基于子类继承）

当目标类**没有实现任何接口**时，JDK 代理无法使用。CGLIB 通过**生成目标类的子类**来实现代理，不要求接口。

```java
import net.sf.cglib.proxy.*;

class LogMethodInterceptor implements MethodInterceptor {
    public Object intercept(Object obj, Method method, Object[] args, MethodProxy proxy) throws Throwable {
        System.out.println("[CGLIB] 方法开始");
        Object result = proxy.invokeSuper(obj, args);   // 调用父类（原方法）
        System.out.println("[CGLIB] 方法结束");
        return result;
    }
}
// 使用
Enhancer enhancer = new Enhancer();
enhancer.setSuperclass(UserServiceImpl.class);          // 设置父类（目标类）
enhancer.setCallback(new LogMethodInterceptor());
UserServiceImpl proxy = (UserServiceImpl) enhancer.create();
proxy.save();
```

#### 三种代理对比

| 代理方式 | 实现机制 | 是否需要接口 | 性能 | 典型应用 |
| --- | --- | --- | --- | --- |
| 静态代理 | 编译时手写代理类 | 否 | 高 | 简单场景 |
| JDK 动态代理 | 反射 + 接口 | **是** | 中 | Spring AOP（默认，目标有接口时） |
| CGLIB 动态代理 | 字节码生成子类 | 否 | 较高 | Spring AOP（目标无接口时） |

> 💡 **提示：** Spring AOP 底层就是动态代理：目标类实现了接口用 JDK 代理，否则用 CGLIB。

---

### 4. 外观模式（Facade）⭐⭐⭐⭐（简略）

**是什么：** 为子系统中的一组接口提供一个**一致的界面（统一入口）**，屏蔽子系统内部复杂性。

**场景类比：** 电脑的「一键开机」按钮——你按一下，背后其实依次启动 CPU、内存、硬盘、显卡，但外观模式把这些都藏起来了。

```java
// 子系统：各自独立
class CPU { void start() { System.out.println("CPU 启动"); } }
class Memory { void start() { System.out.println("内存启动"); } }
class Disk { void start() { System.out.println("硬盘启动"); } }

// 外观：提供统一简化接口
class ComputerFacade {
    private CPU cpu = new CPU();
    private Memory memory = new Memory();
    private Disk disk = new Disk();
    public void startup() {          // 一键开机
        cpu.start();
        memory.start();
        disk.start();
    }
}
// 客户端
new ComputerFacade().startup();
```

### 5. 其他结构型模式（简略）

| 模式 | 一句话 | 例子 |
| --- | --- | --- |
| 享元模式 | 共享对象节省内存 | `Integer.valueOf` 缓存、`String` 常量池 |
| 组合模式 | 树形结构统一处理整体与部分 | 文件系统（文件 + 文件夹）、菜单树 |
| 桥接模式 | 分离抽象与实现两个维度独立变化 | 不同品牌 × 不同颜色 |

---

## 六、行为型模式详解

行为型模式关注**对象之间的职责分配和通信方式**——谁做什么、怎么协作。

### 1. 策略模式（Strategy）⭐⭐⭐⭐⭐

**是什么：** 定义一系列算法，把它们各自封装成类，并且使它们**可以互相替换**。策略模式让算法的变化独立于使用算法的客户端。

**场景类比：** 出行方式——同样的「从 A 到 B」，可以选公交、地铁、打车、骑行（不同策略）。你随时可以切换策略，不影响「出行」这件事本身。

**最大价值：消除大量的 `if-else`。**

#### 类图简图

```
┌─────────────────┐  uses   ┌──────────────────┐
│   Context       │────────▶│  <<interface>>   │
│ -strategy       │         │   Strategy       │
│ +execute()      │         │ +algorithm()     │
└─────────────────┘         └────────┬─────────┘
                                     △
                                     │ implements
                  ┌──────────────────┼──────────────────┐
            ┌─────┴─────┐       ┌─────┴─────┐      ┌─────┴─────┐
            │ StrategyA │       │ StrategyB │      │ StrategyC │
            └───────────┘       └───────────┘      └───────────┘
```

#### 代码示例：支付方式选择

```java
// 抽象策略
interface PaymentStrategy { void pay(double amount); }
// 具体策略：微信支付
class WechatPay implements PaymentStrategy {
    public void pay(double amount) { System.out.println("微信支付 " + amount + " 元"); }
}
// 具体策略：支付宝
class Alipay implements PaymentStrategy {
    public void pay(double amount) { System.out.println("支付宝支付 " + amount + " 元"); }
}
// 具体策略：银行卡
class CardPay implements PaymentStrategy {
    public void pay(double amount) { System.out.println("银行卡支付 " + amount + " 元"); }
}
// 上下文：持有策略，可动态切换
class PaymentContext {
    private PaymentStrategy strategy;
    public void setStrategy(PaymentStrategy s) { this.strategy = s; }
    public void checkout(double amount) { strategy.pay(amount); }
}

// 使用
PaymentContext ctx = new PaymentContext();
ctx.setStrategy(new WechatPay());   ctx.checkout(100);   // 微信
ctx.setStrategy(new Alipay());      ctx.checkout(100);   // 支付宝
```

**对比 if-else 写法：**

```java
// ❌ 每加一种支付方式都要改这里，违背开闭原则
void checkout(String type, double amount) {
    if ("wechat".equals(type)) { /* 微信 */ }
    else if ("alipay".equals(type)) { /* 支付宝 */ }
    else if ("card".equals(type)) { /* 银行卡 */ }
}

// ✅ 用策略模式：新增支付方式只需新增策略类
```

> 💡 **提示：** JDK 的 `Comparator` 就是策略模式——你给 `Collections.sort` 传不同的比较策略，排序算法不变，比较规则可换。

---

### 2. 模板方法模式（Template Method）⭐⭐⭐⭐⭐

**是什么：** 在一个方法中定义算法的**骨架**，而将一些步骤延迟到子类中实现。模板方法让子类在不改变算法结构的情况下，重新定义算法中的某些步骤。

**场景类比：** 菜谱「做菜」——流程固定是「备料 → 起锅烧油 → 下锅炒 → 装盘」（骨架），但具体炒什么菜、放什么调料由子类（番茄炒蛋、青椒肉丝）决定。

#### 类图简图

```
┌─────────────────────────────────┐
│      AbstractClass (抽象类)      │
├─────────────────────────────────┤
│ + templateMethod() {            │  ← final，定义流程骨架
│     step1();                    │
│     step2();   ← 钩子/抽象方法   │
│     step3();                    │
│   }                             │
│ # step1()  { 默认实现 }          │
│ # step2()  { abstract }         │  ← 子类必须实现
│ # step3()  { 默认实现 }          │
└────────────┬────────────────────┘
             △ extends
     ┌───────┴────────┐
┌─────┴─────┐    ┌─────┴─────┐
│ ConcreteA │    │ ConcreteB │   ← 只重写需要变化的步骤
└───────────┘    └───────────┘
```

#### 代码示例：冲泡饮料

```java
// 抽象模板类
abstract class CaffeineBeverage {
    // 模板方法：用 final 锁定流程，子类不能改顺序
    public final void prepare() {
        boilWater();       // 共性步骤，父类实现
        brew();            // 差异步骤，子类实现
        pourInCup();       // 共性步骤
        addCondiments();   // 差异步骤，子类实现
    }
    private void boilWater() { System.out.println("烧水"); }
    private void pourInCup() { System.out.println("倒入杯中"); }
    protected abstract void brew();          // 抽象：子类决定怎么泡
    protected abstract void addCondiments(); // 抽象：子类决定加什么料
}
// 具体子类：泡茶
class Tea extends CaffeineBeverage {
    protected void brew() { System.out.println("用沸水浸泡茶叶"); }
    protected void addCondiments() { System.out.println("加柠檬"); }
}
// 具体子类：冲咖啡
class Coffee extends CaffeineBeverage {
    protected void brew() { System.out.println("用沸水冲泡咖啡粉"); }
    protected void addCondiments() { System.out.println("加糖和牛奶"); }
}
// 使用
new Tea().prepare();      // 烧水→泡茶→倒杯→加柠檬
new Coffee().prepare();   // 烧水→冲咖啡→倒杯→加糖奶
```

**典型应用：** Java 的 `AbstractList`、`HttpServlet` 的 `service()` 方法、Spring 的 `JdbcTemplate`。

---

### 3. 观察者模式（Observer）⭐⭐⭐⭐⭐

**是什么：** 定义对象间的**一对多**依赖关系：当一个对象（主题/被观察者）的状态发生变化时，所有依赖于它的对象（观察者）都会得到通知并自动更新。

**场景类比：** 微信公众号——你关注了某个公众号（注册成为观察者），公众号发文时所有粉丝都会收到推送（通知）。Vue 的「响应式」底层就是观察者模式。

#### 类图简图

```
┌──────────────────┐  notify   ┌──────────────────┐
│  Subject(主题)   │──────────▶│ <<interface>>    │
│ -observers:List  │ register  │  Observer        │
│ +attach()        │◀──────────│ +update()        │
│ +detach()        │           └────────┬─────────┘
│ +notify()        │                    △
└────────┬─────────┘            ┌───────┴────┬────────┐
         △ extends              │            │        │
┌────────┴─────────┐     ┌──────┴───┐ ┌──────┴─┐ ┌────┴───┐
│ ConcreteSubject  │     │ ObserverA│ │ ObsB   │ │ ObsC   │
│ +getState()      │     └──────────┘ └────────┘ └────────┘
└──────────────────┘
```

#### 代码示例：天气广播

```java
import java.util.*;

// 观察者接口
interface Observer { void update(float temperature); }
// 主题（被观察者）
class WeatherStation {
    private final List<Observer> observers = new ArrayList<>();   // 观察者列表
    private float temperature;
    public void attach(Observer o) { observers.add(o); }          // 注册
    public void detach(Observer o) { observers.remove(o); }       // 注销
    public void setTemperature(float t) {
        this.temperature = t;
        notifyAllObservers();                                     // 状态变化时通知
    }
    private void notifyAllObservers() {
        for (Observer o : observers) o.update(temperature);
    }
}
// 具体观察者：手机显示屏
class PhoneDisplay implements Observer {
    public void update(float t) { System.out.println("手机显示：当前温度 " + t + "℃"); }
}
// 具体观察者：窗户显示屏
class WindowDisplay implements Observer {
    public void update(float t) { System.out.println("窗户显示：当前温度 " + t + "℃"); }
}

// 使用
WeatherStation station = new WeatherStation();
station.attach(new PhoneDisplay());
station.attach(new WindowDisplay());
station.setTemperature(26.5f);   // 温度变化，两个显示屏同时更新
```

**典型应用：** Spring 事件机制（`ApplicationEvent` + `ApplicationListener`）、Java 的 `PropertyChangeListener`、Vue 响应式系统、GUI 按钮点击监听。

---

### 4. 责任链模式（Chain of Responsibility）⭐⭐⭐⭐⭐

**是什么：** 使多个对象都有机会处理请求，从而避免请求的发送者和接收者之间的耦合关系。把这些对象连成一条**链**，并沿着这条链传递请求，直到有一个对象处理它为止。

**场景类比：** 公司审批报销——员工提交报销单，先到组长，组长权限不够往上交给经理，经理权限不够再交给总经理……一级级往上，直到有人能拍板。

#### 类图简图

```
┌──────────────────────────────────────┐
│          Handler (抽象处理器)          │
├──────────────────────────────────────┤
│ - next: Handler    ◇(指向下一个)      │
│ + setNext(handler)                   │
│ + handle(request) {                   │
│     if 能处理 → 处理                  │
│     else if next != null → next.handle│
│   }                                   │
└──────────────────┬───────────────────┘
                   △ extends
    ┌──────────────┼──────────────┐
┌────┴────┐   ┌────┴────┐   ┌────┴────┐
│ Handler1│──▶│ Handler2│──▶│ Handler3│   组成一条链
└─────────┘   └─────────┘   └─────────┘
```

#### 代码示例：审批流程

```java
// 抽象处理器
abstract class Approver {
    protected Approver next;     // 下一个处理者
    protected String name;
    public Approver(String name) { this.name = name; }
    public Approver setNext(Approver next) { this.next = next; return next; }  // 链式串联
    public abstract void approve(double amount);
}
// 组长：权限 1000
class TeamLeader extends Approver {
    public TeamLeader(String n) { super(n); }
    public void approve(double amount) {
        if (amount <= 1000) System.out.println(name + " 批准 " + amount + " 元");
        else if (next != null) next.approve(amount);
    }
}
// 经理：权限 10000
class Manager extends Approver {
    public Manager(String n) { super(n); }
    public void approve(double amount) {
        if (amount <= 10000) System.out.println(name + " 批准 " + amount + " 元");
        else if (next != null) next.approve(amount);
    }
}
// 总经理：权限无限
class GeneralManager extends Approver {
    public GeneralManager(String n) { super(n); }
    public void approve(double amount) { System.out.println(name + " 批准 " + amount + " 元"); }
}

// 使用：组装责任链
Approver chain = new TeamLeader("组长");
chain.setNext(new Manager("经理")).setNext(new GeneralManager("总经理"));
chain.approve(500);     // 组长 批准 500 元
chain.approve(5000);    // 经理 批准 5000 元
chain.approve(50000);   // 总经理 批准 50000 元
```

**典型应用：** Servlet 的 `FilterChain`、Spring MVC 的 `HandlerInterceptor`、Netty 的 `ChannelPipeline`、Spring Security 的过滤器链、OkHttp 的拦截器。

---

### 5. 其他行为型模式（简略）

| 模式 | 一句话 | 典型应用 |
| --- | --- | --- |
| 命令模式 | 把「请求」封装成对象，支持撤销/排队 | Runnable、GUI 按钮动作 |
| 迭代器模式 | 顺序访问集合元素，不暴露内部结构 | `java.util.Iterator`、增强 for |
| 状态模式 | 状态驱动行为变化，消除状态 if-else | 订单状态机、工作流 |
| 中介者模式 | 用中介对象解耦多对象间的网状交互 | 聊天室、MVC 的 Controller、机场调度 |
| 备忘录模式 | 保存对象历史状态以支持恢复 | 编辑器撤销、游戏存档 |
| 访问者模式 | 在不修改类的前提下给类增加操作 | 编译器 AST 处理（少用） |
| 解释器模式 | 给定一个语言，定义其文法解释器 | 正则、SQL 解析、EL 表达式（少用） |

---

## 七、设计模式在 JDK / Spring 中的应用

设计模式不是空中楼阁，它广泛存在于主流框架中。下表是高频出现的实战案例：

| 模式 | JDK / Spring 中的应用 |
| --- | --- |
| 单例 | `Runtime.getRuntime()`、`System` 的某些工具、Spring 容器中默认 `scope=singleton` 的 Bean |
| 工厂方法 | `Calendar.getInstance()`、`LoggerFactory.getLogger()`、Spring 的 `BeanFactory` / `ApplicationContext` |
| 抽象工厂 | `java.sql.Connection`（创建 Statement、PreparedStatement 等） |
| 建造者 | `StringBuilder`、`StringBuffer`、`Stream.Builder`、Lombok `@Builder`、OkHttp `Request.Builder` |
| 原型 | `Object.clone()`、`ArrayList` 的 clone |
| 适配器 | `InputStreamReader`（字节流→字符流）、`java.util.Arrays.asList()` |
| 装饰器 | Java IO 流体系（`InputStream` → `FilterInputStream` → `BufferedInputStream`）、`Collections` 的 `synchronizedList` |
| 代理 | Spring AOP（JDK 动态代理 / CGLIB）、`Remote`（RMI 存根）、MyBatis Mapper 接口代理 |
| 外观 | SLF4J 的 `Logger`、Spring 的 `JdbcTemplate`（封装 JDBC 复杂操作） |
| 享元 | `Integer.valueOf` 的 `-128~127` 缓存、`String` 常量池、`Boolean.valueOf` |
| 组合 | `java.awt.Container`、Swing 组件树、Spring `CompositeConfiguration` |
| 桥接 | JDBC 的 `DriverManager`（Driver 接口与具体数据库实现分离） |
| 策略 | `Comparator`、`ThreadPoolExecutor` 的 `RejectedExecutionHandler`、Spring 的 `Resource` |
| 模板方法 | `AbstractList`、`HttpServlet.service()`、Spring `JdbcTemplate`、`RestTemplate` |
| 观察者 | Spring `ApplicationEvent` / `ApplicationListener`、`PropertyChangeListener`、AWT 事件监听 |
| 责任链 | Servlet `FilterChain`、Spring MVC `HandlerInterceptor`、Netty `ChannelPipeline` |
| 命令 | `Runnable`、`Callable`、Spring `MethodInvocation` |
| 迭代器 | `java.util.Iterator`、增强 for 循环底层 |
| 状态 | Spring 状态机、`Thread` 的状态流转 |
| 中介者 | Spring MVC 的 `DispatcherServlet`（中介所有组件） |
| 备忘录 | `Date` 的可变性历史、事务回滚（`TransactionStatus` 保存点） |

> 💡 **提示：** 阅读框架源码时，带着「这里用了什么模式」的视角去看，会发现框架设计「豁然开朗」。这也是阅读优秀源码训练设计感的高效方法。

---

## 八、设计模式的最佳实践与误区

### 1. 黄金法则：不要过度设计

| 原则 | 含义 | 一句话 |
| --- | --- | --- |
| **KISS** | Keep It Simple, Stupid | 简单优先，能用最直白的方式解决就别上模式 |
| **YAGNI** | You Aren't Gonna Need It | 别为「将来可能的需求」预先设计，真需要时再重构 |
| **三次法则** | Rule of Three | 同样代码重复三次以上，再考虑抽象/模式 |

### 2. 常见误区

| 误区 | 说明 |
| --- | --- |
| ❌ 滥用模式 | 一个简单的 CRUD 接口硬套工厂+策略+责任链，过度设计 |
| ❌ 为炫技而用 | 代码评审时堆砌模式，反而让同事看不懂 |
| ❌ 不理解就照抄 | 不理解模式解决的问题，套错地方制造 bug |
| ❌ 忽视场景 | 比如单例乱用导致单元测试困难、隐藏依赖 |
| ❌ 模式万能 | 模式解决的是「设计灵活性」，解决不了「业务正确性」 |

### 3. 正确心态

```
┌──────────────────────────────────────────────────────┐
│  先理解问题  ──▶  看问题是否匹配某模式  ──▶  再选模式  │
│         （而不是反过来：先有模式再找问题）             │
└──────────────────────────────────────────────────────┘
```

> ⚠️ **注意：** 设计模式是「**重构方向**」而不是「**起步姿势**」。绝大多数项目初期不需要任何模式，等到业务变化触发了痛点（如 if-else 爆炸、类爆炸、难以扩展），再用模式重构。先让它 work，再让它 good。

### 4. 何时该用模式（信号）

| 代码信号 | 推荐模式 |
| --- | --- |
| 大量 `if-else`/`switch` 分支 | 策略模式、状态模式 |
| `new` 关键字散落各处 | 工厂模式 |
| 想给对象动态加功能 | 装饰器模式 |
| 一个全局共享实例 | 单例模式 |
| 流程固定但步骤多变 | 模板方法 |
| 一处变化多处联动 | 观察者模式 |
| 一层层审批/过滤 | 责任链模式 |
| 对象参数多且大多可选 | 建造者模式 |

---

## 九、面试常见问题

### Q1：单例模式有哪几种实现？哪种最优？如何保证线程安全？

**答：** 常见五种——饿汉式、懒汉式（加 synchronized）、双重检查锁（DCL）、静态内部类、枚举。

- **线程安全**：饿汉式靠类加载天然安全；懒汉式需 `synchronized`；DCL 用 `volatile` + 双重检查；静态内部类靠类加载机制；枚举天然安全。
- **最优推荐**：**枚举**（防反射和反序列化攻击，写法最简洁）；其次**静态内部类**和 **DCL**（兼顾懒加载和性能）。
- **DCL 中 `volatile` 的作用**：防止 `new` 操作的指令重排序导致其他线程拿到未初始化完成的对象。

### Q2：工厂方法模式与抽象工厂模式的区别？

**答：**
- **工厂方法**：一个工厂只创建**一种**产品，针对「产品等级结构」（同一类型的不同实现）。新增产品只需新增对应工厂，符合开闭原则。
- **抽象工厂**：一个工厂创建**一族**相关产品（如现代风格家具族），针对「产品族」。适合「成套创建」场景。
- 一句话：工厂方法关注**单一产品的扩展**，抽象工厂关注**产品族的切换**。

### Q3：装饰器模式和代理模式有什么区别？

**答：** 两者结构非常像（都持有目标对象引用并实现同一接口），但**目的不同**：

| 维度 | 装饰器 | 代理 |
| --- | --- | --- |
| 主要目的 | **增强**功能（加新行为） | **控制**访问（加权限/延迟/日志等横切关注点） |
| 关注点 | 调用方关心（调用方主动包装） | 被代理方关心（调用方无感） |
| 典型场景 | Java IO 流 | Spring AOP、远程代理 |

### Q4：策略模式解决了什么问题？请举一个 JDK 的例子。

**答：** 策略模式解决了**算法可互换**问题，本质是**消除大量 if-else 分支**，让算法的变化独立于使用者。
JDK 经典例子：`java.util.Comparator`——给 `Collections.sort(list, comparator)` 传不同的比较器（策略），排序算法不变，排序规则可灵活替换。Lambda 表达式让策略模式写得更简洁。

### Q5：模板方法模式和策略模式有什么区别？

**答：**
- **模板方法**：基于**继承**，父类定义算法骨架（流程），子类重写某些步骤。**流程固定，步骤可变**。
- **策略**：基于**组合**，把整个算法封装成对象，运行时切换。**整个算法可换**。
- 一句话：模板方法重定义**算法的局部步骤**，策略重定义**整个算法**。

### Q6：观察者模式和发布-订阅模式是一回事吗？

**答：** 严格区分的话：
- **观察者模式**：主题（Subject）**直接通知**观察者，二者有直接依赖，通常同步。
- **发布-订阅模式**：发布者和订阅者**互不认识**，通过一个**事件总线/消息代理（Broker）**间接通信，通常异步、跨进程。
- 日常开发中常把两者混用，但分布式场景（如 Kafka、RabbitMQ）严格属于发布-订阅。

### Q7：Spring 框架中用到了哪些设计模式？（高频）

**答：** Spring 是设计模式的「百科全书」：
- **单例**：Bean 默认作用域
- **工厂**：`BeanFactory` / `ApplicationContext`
- **代理**：AOP（JDK 动态代理 / CGLIB）
- **模板方法**：`JdbcTemplate`、`RestTemplate`、`TransactionTemplate`
- **观察者**：`ApplicationEvent` 事件机制
- **责任链**：`HandlerInterceptor`、过滤器
- **适配器**：`HandlerAdapter` 适配不同 Controller
- **装饰器**：BeanWrapper、各种 Wrapper
- **策略**：`Resource` 实现、`InstantiationStrategy`

### Q8：为什么要使用设计模式？过度设计有什么危害？

**答：**
- **使用设计模式的好处**：提升代码可维护性、可扩展性、可读性，应对需求变化，提供团队通用词汇。
- **过度设计的危害**：增加系统复杂度（类爆炸、调用链变长）、降低可读性、增加维护成本、拖慢开发进度。
- **平衡原则**：遵循 KISS、YAGNI、三次法则，**先让它跑起来，再让它更优雅**。模式是重构的工具，不是起步的姿势。

### Q9：动态代理 JDK 和 CGLIB 的区别？

**答：**
- **JDK 动态代理**：基于**接口**，目标类必须实现接口；通过 `Proxy.newProxyInstance` + `InvocationHandler` 实现；JDK 自带无需依赖。
- **CGLIB**：基于**继承**（生成目标类子类），不要求接口；通过 `Enhancer` + `MethodInterceptor` 实现；需引入 CGLIB 库。
- **Spring AOP 选择规则**：目标类实现了接口默认用 JDK 代理；否则用 CGLIB；可强制使用 CGLIB（`proxyTargetClass=true`）。
- **性能**：高版本 JDK 下 JDK 动态代理性能已大幅提升，两者差距不明显。

### Q10：开闭原则是什么？如何实现？

**答：** 开闭原则（OCP）：**对扩展开放，对修改关闭**——加新功能时尽量新增代码，不改老代码。
实现手段：通过**抽象（接口/抽象类）+ 多态**。把变化点抽象成接口，新增需求时新增实现类，而不是改老类的逻辑。典型落地模式：策略、工厂、模板方法、装饰器、观察者。

---

## 十、总结

### 核心要点回顾

| 维度 | 要点 |
| --- | --- |
| **本质** | 设计模式 = 面向对象设计原则的具体落地 |
| **灵魂原则** | 单一职责、开闭、里氏替换、依赖倒置、接口隔离、迪米特 |
| **三大分类** | 创建型（5）+ 结构型（7）+ 行为型（11）= GoF 23 种 |
| **高频模式** | 单例、工厂、建造者、适配器、装饰器、代理、策略、模板方法、观察者、责任链 |
| **学习路径** | 理解问题 → 匹配模式 → 编码实践 → 阅读源码印证 |
| **核心心态** | 不过度设计，模式是重构的工具而非炫技 |

### 学习建议

1. **先吃透六大原则**——原则是根，模式是叶。理解原则后，模式自然融会贯通。
2. **每个模式务必手写一遍**——看懂不等于会写，手写过才能内化。
3. **结合框架源码**——在 Spring、JDK 源码中找模式的身影，加深理解。
4. **实战中刻意练习**——日常开发遇到 if-else 爆炸、类爆炸时，主动尝试用模式重构。
5. **遵循 KISS 原则**——别为了用模式而用模式，简单才是最优解。

### 一句话总结

> 设计模式不是教你「怎么写代码」，而是教你「怎么把代码写得更灵活、更易维护」。掌握它，是为了在面对变化时游刃有余，而不是把简单问题复杂化。**先让它 work，再让它 good，最后让它 scale。**
